# Copia este archivo a .env.local (que ya está en .gitignore, nunca se sube)
# y completa el valor real para usar herramientas internas como /categorizar.

# Settings → API → service_role, en tu dashboard de Supabase.
# NUNCA compartas este valor ni lo subas a git.
VITE_SUPABASE_SERVICE_ROLE_KEY=
